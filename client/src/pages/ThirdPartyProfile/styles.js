import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => {
  return {
    userImage: {
      width: "200px",
      height: "200px",
      //   borderRadius: "100%",
      //   objectFit: "cover",
      marginLeft: "20%",
    },
  };
});

export default useStyles;
