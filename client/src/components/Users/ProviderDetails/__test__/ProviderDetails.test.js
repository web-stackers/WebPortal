import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

import ProviderDetails from "../index";

/* test("renders Job Details section", () => {
  render(<ProviderDetails />);
  const jobDetailsElement = screen.getByText(/job details/i);
  expect(jobDetailsElement).toBeInTheDocument();
}); */