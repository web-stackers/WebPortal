import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => {
  return {
    fileUpload: {

    },
    fileInput: {

    },
    label: {

    },
    uploadedFile: {
        display: "block"
    }
  };
});

export default useStyles;
